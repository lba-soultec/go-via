run:
  timeout: 5m
  skip-dirs:
    - vendor
    - statik
    - app/statik

linters:
  disable-all: true
  enable:
    - gofmt
    - govet
    - ineffassign
    - misspell
    - errcheck

issues:
  exclude:
    - "statik"
